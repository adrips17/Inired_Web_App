@model IEnumerable<WebApplicationInired.Models.Comentarios_Marker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_marker)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_marker)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.comentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.comentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.comentario })
        </td>
    </tr>
}

</table>
