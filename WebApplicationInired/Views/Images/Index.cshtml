@model IEnumerable<WebApplicationInired.Models.Images>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.imagen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.imagen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.nombre_marker }) |
            @Html.ActionLink("Details", "Details", new { id=item.nombre_marker }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.nombre_marker })
        </td>
    </tr>
}

</table>
