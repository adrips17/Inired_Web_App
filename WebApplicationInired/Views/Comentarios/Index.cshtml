@model IEnumerable<WebApplicationInired.Models.Comentarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Marker.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Marker.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.comentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.comentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.comentario })
        </td>
    </tr>
}

</table>
